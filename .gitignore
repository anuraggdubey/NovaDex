# ── Dependencies ──
node_modules/

# ── Next.js ──
.next/
out/
build/

# ── Environment (NEVER commit — contains Supabase keys & secrets) ──
.env
.env.local
.env.*.local
.env.production
.env.production.local
.env.development.local

# ── TypeScript ──
tsconfig.tsbuildinfo
*.tsbuildinfo
next-env.d.ts

# ── Supabase ──
supabase/.temp/
supabase/.branches/

# ── Rust / Soroban contracts ──
contracts/target/
**/*.wasm
**/*.d

# ── Stellar CLI keys (never commit) ──
.stellar/
*.secret
*_SECRET*
*_secret*

# ── OS junk ──
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ── Editors & IDEs ──
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ── Debug & logs ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
debug.log
history_debug*.json
apply-theme.js
index.html
metadata.json

# ── Private / local docs (do not push) ──
NovaDEX_v2_Documentation.md
DEPLOYMENT_GUIDE.md

# ── Misc ──
*.tgz
.vercel
.turbo
coverage/
